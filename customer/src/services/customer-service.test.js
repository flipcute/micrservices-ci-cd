//which service is it
describe("Customer Service", () => {
  //which function we are testing

  describe("SignIn", () => {
    // which scenario we are testing 123
    test("validate user inputs", () => {});

    // just a title
    test("validate response", () => {});
  });
});
